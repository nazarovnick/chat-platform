{
  "swagger": "2.0",
  "info": {
    "title": "service.proto",
    "version": "version not set"
  },
  "tags": [
    {
      "name": "ChatService"
    }
  ],
  "consumes": [
    "application/json"
  ],
  "produces": [
    "application/json"
  ],
  "paths": {},
  "definitions": {
    "grpcGetMessagesResponse": {
      "type": "object",
      "properties": {
        "messages": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/grpcMessage"
          },
          "description": "List of messages."
        }
      },
      "description": "Response with a list of messages."
    },
    "grpcMessage": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "format": "uint64",
          "description": "Unique identifier of the message."
        },
        "chat_id": {
          "type": "string",
          "format": "int64",
          "description": "Chat ID where the message was sent."
        },
        "user_id": {
          "type": "string",
          "format": "int64",
          "description": "User ID who sent the message."
        },
        "content": {
          "type": "string",
          "description": "Content of the message."
        },
        "timestamp": {
          "type": "string",
          "format": "int64",
          "description": "Timestamp when the message was sent (Unix time)."
        }
      },
      "description": "Message represents a chat message.",
      "required": [
        "id",
        "chat_id",
        "user_id",
        "content",
        "timestamp"
      ]
    },
    "grpcSendMessageResponse": {
      "type": "object",
      "properties": {
        "message_id": {
          "type": "string",
          "format": "int64",
          "description": "ID of the newly created message."
        }
      },
      "description": "Response after a message is successfully sent.",
      "required": [
        "message_id"
      ]
    },
    "protobufAny": {
      "type": "object",
      "properties": {
        "@type": {
          "type": "string"
        }
      },
      "additionalProperties": {}
    },
    "rpcStatus": {
      "type": "object",
      "properties": {
        "code": {
          "type": "integer",
          "format": "int32"
        },
        "message": {
          "type": "string"
        },
        "details": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/protobufAny"
          }
        }
      }
    }
  }
}
